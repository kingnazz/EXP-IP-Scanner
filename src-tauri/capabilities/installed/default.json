{
  "$schema": "../../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege capability set for the installed EXP IP Scanner window: core IPC and events, the save dialog for CSV export, and the updater with its restart. External URLs, launchers and file writes all go through dedicated Rust commands that validate their input, so the webview is granted no shell, filesystem or opener permission of its own.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-save", "updater:default", "process:allow-restart"]
}
